# /public/audio

Drop your voice intro MP3 here as `intro.mp3` and the Voice Player will auto-detect and play it. Recommended length: 60-90 seconds.

If absent, the Voice Player falls back to /api/voice (ElevenLabs), then to the browser's built-in Web Speech API.
